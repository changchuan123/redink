# 私有仓库部署解决方案

## 🔒 私有仓库部署问题

如果仓库是私有的，Zeabur 需要特殊权限才能访问。

## 方案一：保持私有 + 正确授权（推荐）

### 步骤 1：检查 GitHub 授权

1. 访问：https://github.com/settings/applications
2. 查看 "Authorized OAuth Apps"
3. 找到 **Zeabur**
4. 确认权限包含：
   - ✅ `repo` - 完整仓库访问权限
   - ✅ `read:org` - 组织访问权限（如果需要）

### 步骤 2：重新授权 Zeabur

如果 Zeabur 不在授权列表中，或权限不足：

1. **在 Zeabur 中**：
   - 进入项目设置
   - 找到 "来源" 或 "Source" 设置
   - 点击 "解除绑定 GitHub 仓库"
   - 点击 "连接 GitHub"
   - **重新授权时，确保勾选所有仓库访问权限**

2. **授权时注意**：
   - 选择 "授权访问所有仓库" 或 "授权访问特定仓库"
   - 如果选择特定仓库，确保包含 `changchuan123/redink`

### 步骤 3：使用 Personal Access Token（如果 OAuth 失败）

如果 OAuth 授权一直失败，可以使用 Token：

1. **创建 GitHub Token**：
   - 访问：https://github.com/settings/tokens
   - 点击 "Generate new token (classic)"
   - 名称：`Zeabur Deploy`
   - 过期时间：选择 "No expiration" 或设置较长时间
   - 权限选择：
     - ✅ `repo` - 完整仓库访问权限
   - 生成并**立即复制 Token**（只显示一次）

2. **在 Zeabur 中使用 Token**：
   - 在项目设置中找到 "源代码" 设置
   - 选择 "使用 Personal Access Token"
   - 粘贴 Token
   - 输入仓库：`changchuan123/redink`
   - 输入分支：`main`

## 方案二：改为公开仓库（最简单）

如果代码不涉及敏感信息，改为公开仓库最简单：

### 步骤：

1. **访问仓库设置**：
   - 访问：https://github.com/changchuan123/redink/settings
   - 滚动到底部 "Danger Zone"
   - 点击 "Change visibility"
   - 选择 "Make public"
   - 确认操作

2. **在 Zeabur 中**：
   - 重新连接 GitHub 仓库
   - 应该可以正常访问了

### 公开仓库的优势：

- ✅ Zeabur 可以直接访问，无需特殊授权
- ✅ 部署更稳定，不会因为授权问题失败
- ✅ 其他人可以查看代码（如果这是你想要的）

### 公开仓库的注意事项：

- ⚠️ 代码对所有人可见
- ⚠️ 如果包含敏感信息（API Key 等），需要移除
- ✅ 但环境变量中的 API Key 不会暴露（Zeabur 环境变量是私有的）

## 💡 推荐方案

### 如果代码不敏感 → 改为公开（最简单）

**优点**：
- 部署最稳定
- 无需复杂的授权配置
- 环境变量中的 API Key 仍然是安全的

**操作**：
1. 在 GitHub 仓库设置中改为公开
2. 在 Zeabur 中重新连接仓库
3. 完成

### 如果必须保持私有 → 使用 Token（最可靠）

**优点**：
- 保持仓库私有
- 部署稳定
- 权限控制精确

**操作**：
1. 创建 GitHub Personal Access Token
2. 在 Zeabur 中使用 Token 连接仓库
3. 完成

## 🔍 检查清单

### 方案一（保持私有）：
- [ ] Zeabur 已授权访问 GitHub
- [ ] 授权包含 `repo` 权限
- [ ] 仓库在授权范围内
- [ ] 或使用 Personal Access Token

### 方案二（改为公开）：
- [ ] 仓库已改为公开
- [ ] 确认代码中没有敏感信息
- [ ] 在 Zeabur 中重新连接仓库

## 📝 我的建议

**推荐改为公开仓库**，因为：
1. 代码已经推送到 GitHub，没有特别敏感的信息
2. API Key 都在环境变量中，不会暴露
3. 部署更简单、更稳定
4. 如果以后需要私有，随时可以改回来

**如果必须保持私有**，使用 Personal Access Token 是最可靠的方式。

## ❓ 你的选择

请告诉我：
1. **改为公开** - 我帮你确认步骤
2. **保持私有 + 使用 Token** - 我帮你创建 Token 并配置
3. **保持私有 + 重新授权** - 我帮你检查授权配置

你选择哪个方案？

