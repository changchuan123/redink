# 部署后验证步骤

## ✅ 构建完成

从日志看，Zeabur 构建已完成：
- ✅ 所有文件已上传
- ✅ 构建成功
- ✅ 服务正在启动

## 🔍 验证步骤

### 第一步：查看启动日志

1. **在 Zeabur 中**：
   - 进入项目 → 点击服务
   - 点击 **"日志"** 或 **"Runtime Logs"** 标签
   - **向上滚动到最顶部**，查看服务启动时的日志

2. **应该看到的日志**：

```
🚀 正在启动 红墨 AI图文生成器...
📋 检查配置文件...
✅ 文本生成配置: 激活=deepseek, 可用服务商=['deepseek', 'gemini']
✅ 图片生成配置: 激活=gemini, 可用服务商=['gemini']
✅ 配置检查完成
🔍 检查同步服务配置...
   - SYNC_ENABLED: True
   - COS_SERVICE_URL: http://212.64.57.87:10009/upload-to-cos
   - NOTION_INTEGRATION_TOKEN: 已设置
   - NOTION_DATABASE_ID: 已设置
✅ 同步服务已启用
✅ Notion 同步服务已初始化: database_id=2c771735...
```

### 第二步：验证同步功能

1. **生成一篇笔记和图片**：
   - 访问 Zeabur 提供的域名
   - 输入一个主题，生成笔记和图片
   - 等待生成完成

2. **查看同步日志**：
   - 在日志中搜索 `completed` 或 `同步`
   - 应该看到：
     ```
     📋 检测到历史记录状态更新为 completed: {record_id}
     🔄 已触发记录 {record_id} 的自动同步（后台异步执行）
     🔄 开始同步记录到 Notion: {record_id}
     ✅ 图片 0.png 上传成功
     ✅ 图片 1.png 上传成功
     ...
     ✅ Notion 页面创建成功
     ✅ 记录 {record_id} 自动同步成功
     ```

3. **检查 Notion 数据库**：
   - 访问 Notion 数据库
   - 应该看到新创建的页面
   - 页面应该包含标题、笔记内容和图片

## ⚠️ 如果启动日志显示问题

### 问题 1：显示 "同步服务已禁用"

**可能原因**：
- `SYNC_ENABLED=false` 或未设置
- `NOTION_INTEGRATION_TOKEN` 或 `NOTION_DATABASE_ID` 未设置

**解决方法**：
- 检查环境变量是否都已设置
- 重新部署服务

### 问题 2：显示 "Notion 同步服务未初始化"

**可能原因**：
- `NOTION_INTEGRATION_TOKEN` 或 `NOTION_DATABASE_ID` 为空
- 环境变量值有误

**解决方法**：
- 检查环境变量值是否正确
- 确认没有多余的空格或换行
- 重新部署服务

### 问题 3：没有看到启动日志

**可能原因**：
- 日志被截断
- 服务启动失败

**解决方法**：
- 在 Zeabur 中查看完整的启动日志
- 检查服务状态是否正常

## 📋 检查清单

- [ ] 服务已启动（查看服务状态）
- [ ] 启动日志显示 "同步服务已启用"
- [ ] 启动日志显示 "Notion 同步服务已初始化"
- [ ] 可以访问应用（访问 Zeabur 提供的域名）
- [ ] 可以生成笔记和图片
- [ ] 生成完成后，日志中有同步相关记录
- [ ] Notion 数据库中有新创建的页面

## 🎯 下一步

1. **查看启动日志**，确认同步服务已初始化
2. **测试生成功能**，生成一篇笔记和图片
3. **查看同步日志**，确认是否自动同步
4. **检查 Notion 数据库**，确认页面已创建

如果遇到问题，请提供：
- 启动日志（特别是同步服务相关的部分）
- 生成笔记后的日志
- 具体的错误信息

