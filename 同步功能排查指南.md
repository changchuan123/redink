# 同步功能排查指南

## 🔍 问题：同步功能未执行

如果发现同步功能没有执行，请按以下步骤排查：

## 1. 检查启动日志

服务启动时，应该看到以下日志：

### ✅ 正常情况

```
🚀 正在启动 红墨 AI图文生成器...
📋 检查配置文件...
✅ 文本生成配置: 激活=deepseek, 可用服务商=['deepseek', 'gemini']
✅ 图片生成配置: 激活=gemini, 可用服务商=['gemini']
✅ 配置检查完成
✅ 同步服务已启用
✅ Notion 同步服务已初始化: database_id=2c771735...
```

### ❌ 异常情况

如果看到以下日志，说明配置有问题：

```
⚠️  同步服务已禁用（SYNC_ENABLED=false 或配置未设置）
```

或

```
⚠️  Notion 同步服务未初始化，请检查环境变量配置
```

## 2. 检查环境变量

在 Zeabur 中确认以下环境变量已正确配置：

```bash
SYNC_ENABLED=true
NOTION_INTEGRATION_TOKEN=你的Notion Token
NOTION_DATABASE_ID=2c771735fbc5802e8474d17cda149056
COS_SERVICE_URL=http://212.64.57.87:10009/upload-to-cos
```

**检查方法**：
1. 进入 Zeabur 项目设置
2. 查看"环境变量"标签
3. 确认所有变量都已设置

## 3. 检查历史记录更新日志

当图片生成完成，历史记录状态更新为 `completed` 时，应该看到：

```
📋 检测到历史记录状态更新为 completed: {record_id}
🔄 已触发记录 {record_id} 的自动同步（后台异步执行）
🔄 开始同步记录到 Notion: {record_id}
✅ 图片 0.png 上传成功
✅ 图片 1.png 上传成功
✅ Notion 页面创建成功: {page_id}
✅ 记录 {record_id} 自动同步成功
   - COS URLs: 15 张图片
   - Notion 页面: https://www.notion.so/...
```

## 4. 常见问题排查

### 问题 1：没有看到"检测到历史记录状态更新为 completed"

**可能原因**：
- 历史记录状态不是 `completed`
- 前端没有正确调用更新 API

**解决方法**：
- 检查前端代码是否正确更新状态
- 查看前端网络请求，确认 `PUT /api/history/{record_id}` 请求中 `status` 是否为 `completed`

### 问题 2：看到"同步服务已禁用"

**可能原因**：
- `SYNC_ENABLED` 环境变量未设置或为 `false`
- Notion 配置未设置

**解决方法**：
- 在 Zeabur 环境变量中设置 `SYNC_ENABLED=true`
- 确认 `NOTION_INTEGRATION_TOKEN` 和 `NOTION_DATABASE_ID` 已设置

### 问题 3：看到"Notion 同步服务未初始化"

**可能原因**：
- `NOTION_INTEGRATION_TOKEN` 或 `NOTION_DATABASE_ID` 未设置
- 环境变量值错误

**解决方法**：
- 检查环境变量是否正确设置
- 确认 Token 和 Database ID 没有多余的空格或换行

### 问题 4：同步触发但没有成功

**可能原因**：
- COS 服务不可用
- Notion API 调用失败
- 网络问题

**解决方法**：
- 查看详细错误日志
- 测试 COS 服务：`curl http://212.64.57.87:10009/upload-to-cos`
- 检查 Notion Integration 权限

## 5. 手动测试同步

如果自动同步失败，可以手动触发同步：

```bash
# 使用 curl
curl -X POST https://your-domain.com/api/sync/{record_id}
```

**返回示例**：
```json
{
  "success": true,
  "cos_urls": ["url1", "url2", ...],
  "notion_page_id": "页面ID",
  "notion_page_url": "页面URL"
}
```

## 6. 查看完整日志

在 Zeabur 中查看服务日志：

1. 进入 Zeabur 项目
2. 点击服务
3. 查看"日志"标签
4. 搜索关键词：
   - `同步服务`
   - `Notion`
   - `completed`
   - `自动同步`

## 7. 验证步骤

1. ✅ 启动日志显示"同步服务已启用"
2. ✅ 启动日志显示"Notion 同步服务已初始化"
3. ✅ 生成笔记和图片
4. ✅ 看到"检测到历史记录状态更新为 completed"
5. ✅ 看到"已触发记录 {record_id} 的自动同步"
6. ✅ 看到"记录 {record_id} 自动同步成功"
7. ✅ 在 Notion 数据库中看到新创建的页面

## 📝 日志关键词

搜索以下关键词可以快速定位问题：

- `同步服务` - 同步服务初始化
- `completed` - 历史记录状态更新
- `自动同步` - 同步触发
- `COS` - 图片上传
- `Notion` - Notion 同步
- `⚠️` - 警告信息
- `❌` - 错误信息

