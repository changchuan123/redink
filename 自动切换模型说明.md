# 自动切换模型功能说明

## ✅ 功能实现

**更新时间**: 2025-11-28  
**功能**: 当检测到图片输入但当前模型不支持时，自动切换到支持图片的模型

## 🔄 自动切换逻辑

### 触发条件
1. 用户上传了参考图片（`images` 不为空）
2. 当前激活的文本生成模型不支持图片输入

### 支持的模型类型

**支持图片输入的模型**:
- ✅ Google Gemini (`google_gemini` 类型)
- ✅ GPT-4 Vision (`openai_compatible` 类型，且模型名包含 `gpt-4` 和 `vision`)

**不支持图片输入的模型**:
- ❌ DeepSeek (`openai_compatible` 类型，但模型名不包含 `vision`)
- ❌ 其他纯文本模型

### 切换流程

1. **检测图片输入**
   ```python
   if images and len(images) > 0:
       # 检查当前模型是否支持图片
   ```

2. **检查模型支持**
   ```python
   supports_images = provider_type == 'google_gemini' or (
       provider_type == 'openai_compatible' and 
       'gpt-4' in model.lower() and 'vision' in model.lower()
   )
   ```

3. **自动切换**
   - 如果当前模型不支持图片，查找 Gemini 配置
   - 如果 Gemini 已配置且 API Key 有效，自动切换
   - 重新获取客户端并继续生成

4. **降级处理**
   - 如果找不到支持图片的模型，记录警告并忽略图片
   - 提示词中已包含图片描述信息，不影响生成

## 📋 当前配置

### 文本生成服务商
- **主服务商**: DeepSeek
  - API Key: `你的DeepSeek API Key`
  - Model: `deepseek-chat`
  - 图片支持: ❌ 不支持

- **备用服务商**: Gemini
  - API Key: `你的Gemini API Key`
  - Model: `gemini-2.0-flash`
  - 图片支持: ✅ 支持

## 🎯 使用场景

### 场景 1: 无图片输入
- 使用 DeepSeek 生成大纲
- 快速、经济

### 场景 2: 有图片输入
- 自动切换到 Gemini
- 充分利用图片信息
- 生成更准确的大纲

## 📝 日志示例

**自动切换时的日志**:
```
WARNING: 当前模型 deepseek-chat 不支持图片输入，检测到 1 张参考图片，自动切换到支持图片的模型...
INFO: 切换到 Gemini 模型以支持图片输入
INFO: 已切换到 Gemini: model=gemini-2.0-flash
```

**无可用模型时的日志**:
```
WARNING: 未找到可用的支持图片的模型，将忽略参考图片。提示词中已包含图片描述信息。
```

## 🔧 技术实现

### 修改文件
- `backend/services/outline.py`

### 关键代码
```python
# 如果有图片输入但当前模型不支持，自动切换到支持图片的模型
if images and len(images) > 0 and not supports_images:
    # 查找 Gemini 配置
    gemini_provider = providers.get('gemini')
    
    if gemini_provider and gemini_provider.get('api_key'):
        # 切换到 Gemini
        provider_config = gemini_provider
        model = provider_config.get('model', 'gemini-2.0-flash')
        # 重新获取客户端
        self.client = get_text_chat_client(provider_config)
```

## ✨ 优势

1. **用户体验**: 无需手动切换，自动选择最佳模型
2. **功能完整**: 充分利用图片信息生成大纲
3. **降级处理**: 即使没有支持图片的模型，也能正常工作
4. **日志清晰**: 记录切换过程，便于调试

---

**功能已实现！** 🎉

现在当用户上传参考图片时，系统会自动切换到支持图片的 Gemini 模型，充分利用图片信息生成大纲。

