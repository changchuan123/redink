# 同步功能问题排查清单

## 🔍 当前问题

从日志看，没有看到同步相关的日志，可能的原因：

## 📋 排查步骤

### 1. 查看启动日志（最重要）

在 Zeabur 日志中，**向上滚动到服务启动时**，应该能看到：

```
🚀 正在启动 红墨 AI图文生成器...
📋 检查配置文件...
✅ 文本生成配置: ...
✅ 图片生成配置: ...
✅ 配置检查完成
🔍 检查同步服务配置...
   - SYNC_ENABLED: True/False
   - COS_SERVICE_URL: http://...
   - NOTION_INTEGRATION_TOKEN: 已设置/未设置
   - NOTION_DATABASE_ID: 已设置/未设置
✅ 同步服务已启用
✅ Notion 同步服务已初始化: database_id=2c771735...
```

**如果没有看到这些日志**：
- 说明服务可能没有完全启动
- 或者日志被截断了

### 2. 检查环境变量

在 Zeabur 环境变量中，确认以下变量**都已设置**：

```bash
SYNC_ENABLED=true
NOTION_INTEGRATION_TOKEN=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
NOTION_DATABASE_ID=2c771735fbc5802e8474d17cda149056
COS_SERVICE_URL=http://212.64.57.87:10009/upload-to-cos
```

**检查方法**：
1. 进入 Zeabur 项目设置
2. 查看"环境变量"标签
3. 确认所有变量都已设置
4. **注意**：变量名大小写要完全匹配

### 3. 检查历史记录更新

生成笔记后，在日志中搜索：

- `completed` - 应该看到 "检测到历史记录状态更新为 completed"
- `同步` - 应该看到 "已触发记录...的自动同步"
- `Notion` - 应该看到 Notion 相关日志

**如果没有这些日志**：
- 可能历史记录状态不是 `completed`
- 或者前端没有正确更新状态

### 4. 常见问题

#### 问题 1：启动日志显示 "同步服务已禁用"

**可能原因**：
- `SYNC_ENABLED=false` 或未设置
- `NOTION_INTEGRATION_TOKEN` 未设置
- `NOTION_DATABASE_ID` 未设置

**解决方法**：
- 在 Zeabur 环境变量中设置所有必需的变量
- 重新部署服务

#### 问题 2：启动日志显示 "Notion 同步服务未初始化"

**可能原因**：
- `NOTION_INTEGRATION_TOKEN` 或 `NOTION_DATABASE_ID` 为空
- 环境变量值有误（多余空格、换行等）

**解决方法**：
- 检查环境变量值是否正确
- 确认没有多余的空格或换行
- 重新部署服务

#### 问题 3：没有看到启动日志

**可能原因**：
- 日志被截断
- 服务启动失败

**解决方法**：
- 在 Zeabur 中查看完整的启动日志
- 检查服务是否正常运行

#### 问题 4：生成笔记后没有同步

**可能原因**：
- 历史记录状态不是 `completed`
- 前端没有调用更新 API
- 同步服务未启用

**解决方法**：
- 查看前端网络请求，确认 `PUT /api/history/{record_id}` 请求中 `status` 是否为 `completed`
- 查看后端日志，确认是否收到更新请求

## 🔧 快速验证

### 步骤 1：检查环境变量

在 Zeabur 中确认：
- [ ] `SYNC_ENABLED=true`
- [ ] `NOTION_INTEGRATION_TOKEN` 已设置
- [ ] `NOTION_DATABASE_ID` 已设置
- [ ] `COS_SERVICE_URL` 已设置

### 步骤 2：查看启动日志

在 Zeabur 日志中搜索：
- `同步服务`
- `Notion`
- `SYNC_ENABLED`

应该看到：
- ✅ `同步服务已启用`
- ✅ `Notion 同步服务已初始化`

### 步骤 3：测试同步

1. 生成一篇笔记和图片
2. 等待生成完成
3. 查看日志，搜索 `completed` 或 `同步`
4. 应该看到同步相关的日志

## 📝 需要提供的信息

请提供：

1. **启动日志**（服务启动时的完整日志）
   - 搜索 `同步服务`
   - 看是否有 `✅ 同步服务已启用`

2. **环境变量截图**（如果方便）
   - 确认所有变量都已设置

3. **生成笔记后的日志**
   - 搜索 `completed`
   - 看是否有 `🔄 已触发记录...的自动同步`

4. **前端网络请求**（如果可能）
   - 查看 `PUT /api/history/{record_id}` 请求
   - 确认 `status` 是否为 `completed`

这样我可以更准确地帮你排查问题。

