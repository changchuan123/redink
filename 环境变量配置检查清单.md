# 环境变量配置检查清单

## ✅ 当前已配置的环境变量

```bash
PORT=${WEB_PORT}                                    # ✅ Zeabur 自动设置
PASSWORD=m7c2yg5T3YL4vaiftnp6xeOHw8sz19A0          # ✅ Zeabur 密码
SYNC_ENABLED=true                                   # ✅ 同步服务启用
COS_SERVICE_URL=http://212.64.57.87:10009/upload-to-cos  # ✅ COS 服务地址
NOTION_INTEGRATION_TOKEN=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  # ✅ Notion Token
NOTION_DATABASE_ID=2c771735fbc5802e8474d17cda149056  # ✅ Notion Database ID
```

## ✅ Notion 同步功能配置完整

所有必需的 Notion 同步环境变量都已配置：
- ✅ `SYNC_ENABLED=true` - 启用同步
- ✅ `COS_SERVICE_URL` - COS 服务地址
- ✅ `NOTION_INTEGRATION_TOKEN` - Notion Token
- ✅ `NOTION_DATABASE_ID` - Notion Database ID

## 📋 可选配置（API Key）

如果你想通过环境变量配置 API Key（而不是通过 Web 界面），可以添加：

```bash
DEEPSEEK_API_KEY=你的DeepSeek API Key
GEMINI_API_KEY=你的Gemini API Key
```

**注意**：
- 这些是**可选的**
- 如果不设置，可以通过 Web 界面（`/settings`）配置
- 如果设置了环境变量，会优先使用环境变量的值

## 🔍 配置验证

部署后，查看启动日志，应该看到：

```
🔍 检查同步服务配置...
   - SYNC_ENABLED: True
   - COS_SERVICE_URL: http://212.64.57.87:10009/upload-to-cos
   - NOTION_INTEGRATION_TOKEN: 已设置
   - NOTION_DATABASE_ID: 已设置
✅ 同步服务已启用
✅ Notion 同步服务已初始化: database_id=2c771735...
```

## 📝 总结

### 必需配置（已完整）✅
- ✅ Notion 同步功能的所有环境变量都已配置

### 可选配置
- ⚠️ API Key 可以通过环境变量配置，也可以通过 Web 界面配置
- 如果通过 Web 界面配置，不需要添加 `DEEPSEEK_API_KEY` 和 `GEMINI_API_KEY`

## 🎯 建议

**当前配置已经完整**，可以正常使用 Notion 同步功能。

如果：
- **想通过环境变量配置 API Key**：添加 `DEEPSEEK_API_KEY` 和 `GEMINI_API_KEY`
- **想通过 Web 界面配置 API Key**：不需要添加，部署后访问 `/settings` 页面配置即可

两种方式都可以，选择你喜欢的即可！

